var array = [10, 2, 5, 1, 9];
array.sort(function(a,b) {
  return b - a
});

// Read the above article and write JavaScript code
// to sort the array numerically
// (the sorted array should equal [1, 2, 5, 9, 10])